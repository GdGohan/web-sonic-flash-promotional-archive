}
catch(error:Error)
{
}

import flash.external.ExternalInterface;
private function logConsole(message:String):void
{
   if(ExternalInterface.available)
   {
      ExternalInterface.call(
         "console.log",
         message
      );
   }
}